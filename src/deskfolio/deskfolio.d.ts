import type { ReactElement } from 'react'

/** The DeskFolio birthday wish page (book, desk scene, stickers, scribble underlines). */
export declare function DeskFolioPage(): ReactElement

/** DeskFolioPage wrapped in the page-level layout classes it expects. */
export declare function DeskFolio(): ReactElement
